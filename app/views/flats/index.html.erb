<h1> All Flats</h1>

  <% @flats.each do |flat|  %>
  <div class="flat-card">
    <div class="flat-card-image" style="background-image: url('https://images.unsplash.com/photo-1494526585095-c41746248156?w=1200')"></div>
    <div class="flat-card-content">
      <div class="flat-card-title">
        <h4><%=link_to flat.name, flat_path(flat)%></h4>
      </div>
      <p><%=flat.address%></p>
      <div class="flex">
        <div class="flex align-items-center">
          <i class="fa-solid fa-people-line"></i>
          <span><%=flat.number_of_guests%></span>
        </div>
        <div class="flex align-items-center ms-3">
          <i class="fa-solid fa-euro-sign"></i>
          <span><%=flat.price_per_night%></span>
        </div>
      </div>
    </div>
    <div class="flat-card-actions">
      <a href=""><%=link_to 'More info', flat_path(flat)%></a>
      <a class="ms-3 text-warning" href=""><%= link_to 'Edit', edit_flat_path(flat) %></a>
      <a class="ms-3 text-danger"> <%= link_to "Delete",flat_path(flat),
      data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %>
      </a>
    </div>
  </div>
  <% end %>
